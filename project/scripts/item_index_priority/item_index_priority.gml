for(var w=topleft_cell_x;w<topleft_cell_x+size_width;w++) {
	for(var h=topleft_cell_y;h<topleft_cell_y+size_height;h++) {
		if gridController.grid_items[# w, h] != object_index gridController.grid_items[# w, h] = object_index
		
		
	}
}